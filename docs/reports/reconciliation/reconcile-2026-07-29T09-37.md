# Reconciliation tồn kho — 2026-07-29T09:37:12.487Z

Universe: pallet ∈ [IN_STORAGE, IN_STAGING, CONFIRMED] · 10 dòng · 9 pallet.
**Tổng chung (grand): 93 thùng.** Kết luận: ✓ KHỚP (không ERROR).

## Đối chiếu 5 chiều
| Chiều | Tổng | Nhóm |
|---|---|---|
| D1 — theo SKU | 93 | 2 mã |
| D2 — theo pallet | 93 | 9 pallet |
| D3 — theo location | 93 | 5 vị trí (unlocated 35) |
| D4 — theo lot/HSD | 93 | 8 lô (không HSD 6) |
| D5 — available/blocked | 93 | available 93 + blocked 0 |
| D6 — movement (ledger) | STAGE_OUT:14 | null-qty 6 |

## Cross-check toàn vẹn
| Kiểm tra | Kỳ vọng | Thực tế | Kết quả |
|---|---|---|---|
| D1 SKU = tổng chung | 93 | 93 | ✓ |
| D2 pallet = tổng chung | 93 | 93 | ✓ |
| D3 location(+unlocated) = tổng chung | 93 | 93 | ✓ |
| D4 lot/HSD = tổng chung | 93 | 93 | ✓ |
| D5 available+blocked = tổng vật lý | 93 | 93 | ✓ |

## Chênh lệch (root cause · owner · disposition)
| Mức | Mã | Entity | Chi tiết | Root cause | Owner | Disposition |
|---|---|---|---|---|---|---|
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line fba35a5f-cca8-419a-9253-8abaf2efed04 @ pallet PL260522.001 (VG-NM-500) | units_per_box=1 nhưng qty_unit=480 = qty_box×24 → hệ số quy đổi thực có vẻ là 24. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 1b4756eb-8ee0-4e37-93f1-3c032d9a16a6 @ pallet PL260522.002 (VG-DA-1000) | units_per_box=1 nhưng qty_unit=180 = qty_box×12 → hệ số quy đổi thực có vẻ là 12. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 96b9e8a5-e606-47b0-80de-7c4b84d7c01c @ pallet PL260520.001 (VG-NM-500) | units_per_box=1 nhưng qty_unit=240 = qty_box×24 → hệ số quy đổi thực có vẻ là 24. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 81ede596-88b3-4661-a457-adee868959fa @ pallet PL260520.002 (VG-NM-500) | units_per_box=1 nhưng qty_unit=240 = qty_box×24 → hệ số quy đổi thực có vẻ là 24. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 9843a3a2-fb83-4095-bc71-08bfaedf1183 @ pallet PL260521.001 (VG-DA-1000) | units_per_box=1 nhưng qty_unit=96 = qty_box×12 → hệ số quy đổi thực có vẻ là 12. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line a8a47a03-0a4a-4ad0-bb76-b185181cf211 @ pallet PLMULTI.001 (VG-NM-500) | units_per_box=1 nhưng qty_unit=240 = qty_box×24 → hệ số quy đổi thực có vẻ là 24. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 3fcab14a-22ac-40dc-8e70-584f4de6c4f6 @ pallet PLMULTI.001-P2 (VG-DA-1000) | units_per_box=1 nhưng qty_unit=84 = qty_box×12 → hệ số quy đổi thực có vẻ là 12. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 2abbd45c-75d3-4903-b591-103bc9151836 @ pallet PLDADU.002 (VG-NM-500) | units_per_box=1 nhưng qty_unit=144 = qty_box×24 → hệ số quy đổi thực có vẻ là 24. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| WARN | `UNITS_PER_BOX_UNCONFIGURED` | line 021aaf35-b12d-43af-bf67-5be4b94358e4 @ pallet PLMULTI.001-P1 (VG-DA-1000) | units_per_box=1 nhưng qty_unit=84 = qty_box×12 → hệ số quy đổi thực có vẻ là 12. | ItemCode.units_per_box chưa cấu hình (mặc định 1) trong khi qty_unit đã mang số đơn vị lẻ thật. | OPS/BE | Cấu hình lại units_per_box cho mã hàng (không đổi tồn thùng qty_box → không ảnh hưởng tổng tồn). |
| INFO | `MOVEMENT_NULL_QTY` | movement ledger | 6 movement thiếu qty_box (thường là bản ghi cũ). | Trước WVG-179 movement nguyên-pallet ghi qty=null. | BE | Đã chuẩn hoá từ WVG-179 trở đi; KHÔNG truy hồi sửa bản ghi cũ (append-only). |

> Nguyên tắc: **không chỉnh balance trực tiếp** — mọi chênh lệch xử lý qua điều tra + quy trình Điều chỉnh, gắn owner/disposition.
